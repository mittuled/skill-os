# Scenario: REST CRUD API for Tasks

Build a TypeScript Express API with:

1. CRUD endpoints for a `tasks` resource
2. Input validation with Zod
3. Proper HTTP status codes and error handling
4. TypeScript types for request/response

# Scenario: Structured Logging for Express.js

Set up structured JSON logging for an Express app that:

1. Logs every request with method, path, status, and duration
2. Assigns a unique request ID to each request for tracing
3. Includes error stack traces in log output
4. Uses pino for high-performance structured logging

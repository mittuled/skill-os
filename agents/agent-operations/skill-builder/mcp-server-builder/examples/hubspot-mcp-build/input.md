# Scenario: Build MCP Server for HubSpot CRM API

Build an MCP server from HubSpot CRM OpenAPI spec. 47 endpoints across contacts, companies, deals, tickets, notes, owners. Auth: OAuth2. Need tools for CRUD operations on all resources.

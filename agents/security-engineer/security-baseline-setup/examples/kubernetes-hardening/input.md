# Scenario: Kubernetes Security Baseline

Create a Kubernetes security configuration that:

1. Enforces non-root containers
2. Sets resource limits and read-only root filesystems
3. Blocks privilege escalation
4. Applies a restrictive NetworkPolicy
5. Configures RBAC with least-privilege service account

# Scenario: Phase Gate — Definition to Production for Notification Settings

The notification settings redesign project is completing the definition phase. The PM wants to move the project to production design (visual design and prototyping). The Head of Design is running the phase gate review to confirm the definition phase deliverables meet the exit criteria.

## Input Parameters

```json
{
  "project_name": "Notification Settings Redesign",
  "phase_transition": "definition_to_production",
  "criteria": {
    "user_flows": "pass",
    "wireframes": "pass",
    "content_requirements": "incomplete",
    "accessibility_targets": "pass",
    "design_system_components": "pass"
  },
  "notes": {
    "content_requirements": "Microcopy for notification labels and empty states not yet finalized. Content designer is OOO until next week."
  }
}
```
